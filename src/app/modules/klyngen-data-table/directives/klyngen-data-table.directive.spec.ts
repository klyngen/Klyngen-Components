import { KlyngenDataTableDirective } from './klyngen-data-table.directive';

describe('KlyngenDataTableDirective', () => {
  it('should create an instance', () => {
    const directive = new KlyngenDataTableDirective();
    expect(directive).toBeTruthy();
  });
});
